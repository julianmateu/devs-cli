# v1.1 Polish

Next batch of features for `devs-cli` after v1.0 is stable.

## Planned features

1. **Man pages** -- generate man pages from clap metadata via `clap_mangen`
2. **tmux help** -- `devs tmux-help` quick reference for common tmux operations
3. **Shareable .devs.toml** -- project-local config file that can be checked into a repo

## Status

Planning phase. No implementation started.
